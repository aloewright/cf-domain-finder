interface:
  display_name: "Cf Domain Finder"
  short_description: "Repo-specific patterns and workflows for cf-domain-finder"
  default_prompt: "Use the cf-domain-finder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true